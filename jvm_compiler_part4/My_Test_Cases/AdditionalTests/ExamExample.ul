void main() 
{
	int resultOne;
	int resultTw0;
	
	resultOne = foo(2, 5);


	println resultOne;
	println bar(foo(5, 15),10);
}

int foo(int x, int y)
{
	if(x < y)
	{
		println x;
	}else
	{
		println y;
	}

	return 20;
}

int bar(int x, int y)
{
	int a;
	int b;

	a = 10;
	b = a * 2;

	if(x == b)
	{
		println x;
	}
	return y;
}